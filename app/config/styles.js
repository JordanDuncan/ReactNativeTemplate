/*
 * app/config/styles.js
 * Global colors and styles.
 * Represent these as objects, not stylesheets, so you can import and use them 
 * in conjunction with additional styling.
 */

export const colors = {
  primary : '#009688',
  primaryText : '#E0F2F1',
  fadedText : '#CCC'
};

export const fullFlexContainer = {
  flex: 1,
  flexDirection: 'column'
};

export const centerChildren = {
  justifyContent: 'center',
  alignItems: 'center'
};